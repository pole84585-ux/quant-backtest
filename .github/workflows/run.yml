name: A-share-master-cycle

on:
  schedule:
    - cron: "0 1 * * 1-5"   # 09:00 北京时间
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.10

    - name: Install dependencies
      run: pip install akshare pandas requests

    - name: Run system
      run: python main.py
